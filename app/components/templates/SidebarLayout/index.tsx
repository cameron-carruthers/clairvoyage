import { styled } from 'styled-components';

const SidebarLayout = styled.aside`
  height: 100dvh;
  padding: 2rem;
  overflow: scroll;
  width: 12.5rem;
  background: white;
`;

export default SidebarLayout;